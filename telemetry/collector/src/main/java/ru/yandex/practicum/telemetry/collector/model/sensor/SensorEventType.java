package ru.yandex.practicum.telemetry.collector.model.sensor;

public enum SensorEventType {
    MOTION_SENSOR_PROTO,
    TEMPERATURE_SENSOR_PROTO,
    LIGHT_SENSOR_PROTO,
    CLIMATE_SENSOR_PROTO,
    SWITCH_SENSOR_PROTO,
    PAYLOAD_NOT_SET
}
